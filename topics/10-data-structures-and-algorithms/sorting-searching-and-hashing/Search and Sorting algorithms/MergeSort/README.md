# Merge Sort

Splits input into smaller ranges, sorts them, and merges the sorted ranges.

## Learn

- Divide and conquer.
- Recursive base cases and merge invariants.
- $O(n \log n)$ time and additional memory requirements.

Trace `Merge.cpp` on an odd-sized list and draw the split and merge tree.