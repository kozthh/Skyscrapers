# Insertion Sort

Builds a sorted prefix by inserting each next value into its correct position.

## Learn

- The sorted-prefix mental model.
- Why insertion sort suits small or nearly sorted data.
- In-place shifting and $O(n^2)$ worst-case behavior.

Trace `Insertion.cpp` and count shifts as well as comparisons.