# Encapsulated Bank Account

This Java exercise keeps account state inside an account class and exposes behavior through controlled operations.

## What you can learn

- Encapsulation and access control.
- Why callers should use behavior instead of mutating fields.
- Where domain validation belongs.

Start with `Vault/Main.java`, then test deposits and withdrawals with invalid values.# bank-account

Encapsulation practice project for a simple bank account model.