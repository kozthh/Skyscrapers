# Logger System

This Java exercise switches between console, file, and database-style logging implementations.

## What you can learn

- A common logging contract with multiple implementations.
- Polymorphic dispatch and a selector object.
- The tradeoff between a simple switcher and dependency injection.

Read `Main.java` and `logger/LoggerSwitcher.java`. Add structured logging while keeping the caller unchanged.# logger-system

Polymorphism practice project for logger switching behavior.