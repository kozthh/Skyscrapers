# Constructor Basics

This Java example uses a small bank model to practice object initialization, fields, and simple operations.

## What you can learn

- Constructor overloading and initialization.
- The relationship between constructors and encapsulated state.
- How a main program collaborates with model classes.

Trace `src/Main.java` into `src/bank` and `src/Banker`, then reject an insufficient withdrawal.# constructor-basics

Constructor practice project for the OOP roadmap.